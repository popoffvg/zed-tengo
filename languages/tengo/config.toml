name = "Tengo"
grammar = "tengo"
path_suffixes = ["tengo"]
# tengo-lsp formats with hard tabs (one level per enclosing block), so indent
# with tabs to match — otherwise the editor inserts spaces that formatting then
# rewrites.
hard_tabs = true
tab_size = 4
line_comments = ["// "]
block_comment = ["/* ", " */"]
autoclose_before = ";:.,=}])> \n"
brackets = [
  { start = "{", end = "}", close = true, newline = true },
  { start = "[", end = "]", close = true, newline = true },
  { start = "(", end = ")", close = true, newline = true },
  { start = "\"", end = "\"", close = true, newline = false, not_in = ["string", "comment"] },
  { start = "'", end = "'", close = true, newline = false, not_in = ["string", "comment"] },
  { start = "`", end = "`", close = true, newline = false, not_in = ["string", "comment"] },
]
increase_indent_pattern = "\\{\\s*$"
decrease_indent_pattern = "^\\s*\\}"
